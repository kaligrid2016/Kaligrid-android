<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".activity.MainActivity">

    <!-- Top toolbar -->
    <android.support.design.widget.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/toolbar_top_height"
        android:theme="@style/AppTheme.AppBarOverlay">

        <include layout="@layout/toolbar_top" />

    </android.support.design.widget.AppBarLayout>

    <!-- Content view -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
            android:id="@+id/content_fragment_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </RelativeLayout>

    <!-- Add event button -->
    <android.support.design.widget.FloatingActionButton
        android:id="@+id/button_add_event"
        android:layout_width="@dimen/button_add_event_size"
        android:layout_height="@dimen/button_add_event_size"
        android:layout_gravity="bottom|end"
        android:layout_marginRight="@dimen/button_add_event_margin_right"
        android:layout_marginBottom="@dimen/button_add_event_margin_bottom"
        android:src="@drawable/buttom_float_add_cross"
        android:scaleType="center"
        app:backgroundTint="@color/colorPrimary" />

    <!-- Bottom toolbar -->
    <include layout="@layout/toolbar_bottom" />

</android.support.design.widget.CoordinatorLayout>
