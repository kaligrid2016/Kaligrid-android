<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kaligrid">

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:fullBackupContent="true">

        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".activity.NewEventMenuActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:screenOrientation="portrait" />

        <activity
            android:name=".activity.NewEventActivity"
            android:label="@string/new_event_activity_title"
            android:theme="@style/AppTheme.NewEvent"
            android:screenOrientation="portrait" />

        <activity
            android:name=".activity.NewReminderActivity"
            android:label="@string/new_reminder_activity_title"
            android:theme="@style/AppTheme.NewReminder"
            android:screenOrientation="portrait" />

        <activity
            android:name=".activity.NewFyiActivity"
            android:label="@string/new_fyi_activity_title"
            android:theme="@style/AppTheme.NewFyi"
            android:screenOrientation="portrait" />

    </application>

</manifest>
